{
  "name": "cpr2022",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build-schema && node scripts/md2json.mjs && npm run validate-schema",
    "build-schema": "ts-json-schema-generator --path packages/data/src/types/schema.d.ts --type Constitucion --expose all --out data/json/schema.json",
    "validate-schema": "ajv validate -s data/json/schema.json -d data/json/constitucion.json --allow-union-types"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ajv-cli": "^5.0.0",
    "ts-json-schema-generator": "^1.0.0"
  }
}
